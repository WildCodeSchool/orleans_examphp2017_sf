{% extends 'base.html.twig' %}

{% block body %}
    <h1>Players list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Experience</th>
                <th>Age</th>
                <th>Fatigue</th>
                <th>Role</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for player in players %}
            <tr>
                <td><a href="{{ path('players_show', { 'id': player.id }) }}">{{ player.id }}</a></td>
                <td>{{ player.nom }}</td>
                <td>{{ player.experience }}</td>
                <td>{{ player.age }}</td>
                <td>{{ player.fatigue }}</td>
                <td>{{ player.role }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('players_show', { 'id': player.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('players_edit', { 'id': player.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('players_new') }}">Create a new player</a>
        </li>
    </ul>
{% endblock %}
