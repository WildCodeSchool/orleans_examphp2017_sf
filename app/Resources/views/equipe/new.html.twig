{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block body %}
        <h1 class="text-center">Création d'une équipe</h1>

    <div class="well">
    {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('equipe_index') }}">Back to the list</a>
            </li>
        </ul>
    </div>
{% endblock %}
