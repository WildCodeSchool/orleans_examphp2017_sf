{% extends 'base.html.twig' %}

{% block body %}

    {% for equipe in equipes %}


    <h1>Equipe</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ equipe.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ equipe.nom }}</td>
        </tr>
        <tr>
            <th>Pays</th>
            <td>{{ equipe.pays }}</td>
        </tr>
        </tbody>
    </table>
            <a href="{{ path('team_show', {'id': equipe.id}) }}">choisir</a>

    {% endfor %}

    <a href="{{ path('jouer') }}">jouer</a>

{% endblock %}

