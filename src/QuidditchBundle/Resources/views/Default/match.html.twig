{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block body %}
    <h1 class="text-center">Page du match</h1>
        <div class="well">
            {{ form_start(form) }}
                {{ form_widget(form) }}
            <input class="btn btn-primary center-block" type="submit">
            {{ form_end(form) }}
        </div>
    {% for equipe in equipes %}
        {{ equipe.nom }}
    {% endfor %}
{% endblock %}